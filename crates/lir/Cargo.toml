[package]
name = "swamp-lir"
version = "0.2.26"
edition = "2024"
license = "MIT"
repository = "https://github.com/swamp/swamp"
description = "Swamp LIR language"


[dependencies]
swamp-ir-shared = { path = "../ir-shared" }

[features]
debug-info = []
