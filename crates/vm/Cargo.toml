[package]
name = "swamp-vm"
version = "0.1.16"
edition = "2024"
license = "MIT"
description = "Minimial virtual machine"
repository = "https://github.com/swamp/swamp"


[dependencies]
swamp-vm-types = { path = "../vm-types", version = "0.1.16" }

fixed32 = "0.0.17"
squirrel-prng = "0.0.1"
tinter = "0.1.0"


sparse-mem = { path = "../sparse-mem", version = "0.1.16" }
hashmap-mem = { path = "../hashmap-mem", version = "0.1.16" }

[features]
debug_vm = []  # Enable VM debug output
