[package]
name = "swamp-vm"
version = "0.1.16"
edition = "2024"
license = "MIT"
description = "Minimial virtual machine"
repository = "https://github.com/swamp/swamp"


[dependencies]
swamp-vm-types = { path = "../vm-types", version = "0.1.16" }
swamp-vm-disasm = { path = "../vm-disasm", version = "0.1.16" }
swamp-vm-debug-info = { path = "../vm-debug-info", version = " 0.1.16" }

seq-map = "0.0.13"
fixed32 = "0.0.17"
fixed32-math = "0.0.18"
squirrel-prng = "0.0.1"
tinter = "0.1.0"

[features]
debug_vm = []  # Enable VM debug output
